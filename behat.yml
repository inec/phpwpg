default:
    suites:
        default:
            contexts:
            - FeatureContext: features
                username: %behat.paths.features%/bootstrap
                password: notgoingtotellyou